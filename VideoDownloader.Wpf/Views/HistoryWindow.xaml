<Window x:Class="VideoDownloader.Wpf.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:VideoDownloader.Wpf.Localization"
        Title="{loc:Tr 'Download history'}" Height="520" Width="900" MinWidth="650" MinHeight="320"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{loc:Tr 'Search'}" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{Binding View}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{loc:Tr 'Date'}" DisplayMemberBinding="{Binding Date}" Width="130" />
                    <GridViewColumn Header="{loc:Tr 'Title'}" DisplayMemberBinding="{Binding Title}" Width="200" />
                    <GridViewColumn Header="{loc:Tr 'URL'}" DisplayMemberBinding="{Binding Url}" Width="160" />
                    <GridViewColumn Header="{loc:Tr 'Duration'}" DisplayMemberBinding="{Binding Duration}" Width="80" />
                    <GridViewColumn Header="{loc:Tr 'Size'}" DisplayMemberBinding="{Binding Size}" Width="90" />
                    <GridViewColumn Header="{loc:Tr 'Avg. speed'}" DisplayMemberBinding="{Binding Speed}" Width="100" />
                    <GridViewColumn Header="{loc:Tr 'Status'}" DisplayMemberBinding="{Binding Status}" Width="90" />
                    <GridViewColumn Header="{loc:Tr 'Actions'}" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button ToolTip="{loc:Tr 'Redownload'}"
                                            Command="{Binding DataContext.RedownloadCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Width="26" Height="26" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="5">
                                        <Viewbox Stretch="Uniform">
                                            <Path Fill="{DynamicResource ForegroundBrush}" Data="{StaticResource RefreshIconGeometry}" />
                                        </Viewbox>
                                    </Button>
                                    <Button ToolTip="{loc:Tr 'Open in browser'}"
                                            Command="{Binding DataContext.OpenInBrowserCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Width="26" Height="26" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="5">
                                        <Viewbox Stretch="Uniform">
                                            <Path Fill="{DynamicResource ForegroundBrush}" Data="{StaticResource OpenInNewIconGeometry}" />
                                        </Viewbox>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="{loc:Tr 'Clear history'}" Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding ClearHistoryCommand}" />
            <Button Grid.Column="2" Content="{loc:Tr 'Close'}" Style="{StaticResource PrimaryButtonStyle}"
                    Width="100" Click="Close_Click" />
        </Grid>
    </Grid>
</Window>
